#include "main.h"
/**
 * _isalpha - is a function jfd jfnd
 *
 * @c: is an int lkdf fvnk
 *
 * Return: 1 or 0 id kdn kn
*/

int _isalpha(int c)
{
	if (c >= 65 && c <= 90 || c >= 97 && c <= 122)
		return (1);
	else
		return (0);
}
